{
  "name": "encurtador-link",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
